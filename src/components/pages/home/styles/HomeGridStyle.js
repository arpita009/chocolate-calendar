import { styled } from '@mui/system';
import {Grid} from '@mui/material';

const HomeGridStyle = styled(Grid,{})({
//    container,
   justifyContent: 'center',
   alignItems: 'center',
   direction: 'column',
   
});
export default HomeGridStyle; 
