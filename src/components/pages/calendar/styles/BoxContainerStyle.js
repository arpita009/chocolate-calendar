import { styled } from '@mui/system';
import {Paper} from '@mui/material';

const BoxContainerStyle = styled(Paper,{})({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'

});
export default BoxContainerStyle; 