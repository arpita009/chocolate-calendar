import { styled,Box } from '@mui/system';

const BoxCalendarTableStyle = styled(Box,{})({
    display: 'flex',
    flexWrap: 'wrap',
    flexFlow: 'row',
    alignItems: 'center',
    textAlign: 'center'

});
export default BoxCalendarTableStyle; 